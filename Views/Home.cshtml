@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.Home>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
	Layout = "Master.cshtml";
}
<!DOCTYPE HTML>

		<!-- Jumbotron, w title -->
		<!--<div class="jumbotron text-center jumbotron-fluid">-->
		<div class="container-fluid" style="background-image:url('/images/mick-4.JPG'); background-size:cover; mt--2">
			<div class="container pt-5 pb-5">
				<!--<h1 class="display-1">Welcome - UmbracoTV</h1>-->
				<div class ="container" style="margin-left: 0!important;
  margin-right: 0!important; white-space: pre-line">
				<p style="color:white; font-size:2rem"><BR>@Model.Value("title", fallback: Fallback.ToAncestors)</p><BR>
</div>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
			</div>
		</div>

		<!-- Blog section -->
		<div class="container">

			<div class="row section" style="justify-content:space-around;">
				<div class="col-md-12">
					<h2 style="text-align:center;">Recent Projects</h2>
				</div>

				<!-- Blog Post query goes here. -->
				<div class="col-md-5">
					<div class="box-blog" style="height:400px">
						<a href="#"><div class="blog-promoImage" style="background-image:url('/images/Personal_site.png')"></div></a>
						<h5>@Model.Value("blog1Title", fallback: Fallback.ToAncestors)</h5>
						<p>@Model.Value("blog1ShortText", fallback: Fallback.ToAncestors)</p>
					</div>
					<a href="project1" class="btn btn-inverted">@Model.Value("blog1ButtonText", fallback: Fallback.ToAncestors)</a>
				</div>
				<div class="col-md-5">
					<div class="box-blog" style="height:400px">
						<a href="#"><div class="blog-promoImage" style="background-image:url('/images/Eilidhs_Trust_Homepage.png')"></div></a>
						<h5>@Model.Value("blog2Title", fallback: Fallback.ToAncestors)</h5>
						<p>@Model.Value("blog2ShortText", fallback: Fallback.ToAncestors)</p>
					</div>
					<a href="project2" class="btn btn-inverted">@Model.Value("blog2ButtonText", fallback: Fallback.ToAncestors)</a>
				</div>
				<div class="col-md-5">
					<div class="box-blog" style="height:400px">
						<a href="#"><div class="blog-promoImage" style="background-image:url('/images/MovieMatch_homepage.png')"></div></a>
						<h5>@Model.Value("blog3Title", fallback: Fallback.ToAncestors)</h5>
						<p>@Model.Value("blog3ShortText", fallback: Fallback.ToAncestors)</p>
					</div>
					<a href="#" class="btn btn-inverted blog-box-btn">@Model.Value("blog3ButtonText", fallback: Fallback.ToAncestors)</a>
				</div>
					<div class="col-md-5">
					<div class="box-blog" style="height:400px">
						<a href="#"><div class="blog-promoImage" style="background-image:url('/images/hats.png')"></div></a>
						<h5>@Model.Value("blog4Title", fallback: Fallback.ToAncestors)</h5>
						<p>@Model.Value("blog4ShortText", fallback: Fallback.ToAncestors)</p>
					</div>
					<a href="#" class="btn btn-inverted blog-box-btn">@Model.Value("blog4ButtonText", fallback: Fallback.ToAncestors)</a>
				</div>
			</div>

		</div>

